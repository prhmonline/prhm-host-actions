'use strict';
const test=require('node:test');
const assert=require('node:assert/strict');
const crypto=require('node:crypto');
const path=require('node:path');
const IMPL=path.join(__dirname,'control-plane-installer-refresh-l4-binding-repair-v1.js');
const sha=s=>crypto.createHash('sha256').update(s,'utf8').digest('hex');
function load(){delete require.cache[require.resolve(IMPL)];return require(IMPL)}
test('builds dedicated critical L4 binding while preserving generic L3',()=>{const m=load(),OLD='host_action.control_plane_root_scripts_stage_transport_v1',NEW='host_action.control_plane_installer_refresh_root_stage_v1',policy=JSON.stringify({schema_version:'prhm.approval-policy.v1',operations:{[OLD]:{level:3}},typed_scopes:[{tool:'control_plane_root_scripts_stage_transport_apply_v1',project:'control_plane',environment:'production',action:'control_plane_root_scripts_stage_transport_v1',risk:'high',operation:OLD,principals:[{principal_id:'mohammad',roles:['mcp-operator']}]},{tool:'host_action_v2_apply',project:'control_plane',environment:'production',action:'control_plane_root_scripts_stage_transport_v1',risk:'high',operation:OLD,principals:[{principal_id:'mohammad',roles:['mcp-operator']}]}]},null,2)+'\n',mediator=["export const FIXED_BINDING=Object.freeze({","  principal_id:'mohammad',","  role:'mcp-operator',","  tool:'control_plane_root_scripts_stage_transport_apply_v1',","  project:'control_plane',","  environment:'production',","  action:'control_plane_root_scripts_stage_transport_v1',","  risk:'critical',","  operation:'"+OLD+"'","});","export const CONFIRM_LITERAL='CONFIRM_LEVEL_4_CRITICAL';","if(Number(request.level)!==4)throw new Error('request_binding_mismatch');","if(String(second_confirmation||'')!==CONFIRM_LITERAL)throw new Error('critical_second_confirmation_required');"].join('\n')+'\n',out=m.buildCandidates(policy,mediator),p=JSON.parse(out.policy);assert.equal(p.operations[OLD].level,3);assert.equal(p.operations[NEW].level,4);assert.equal(p.operations[NEW].risk,'critical');assert.equal(p.typed_scopes.filter(x=>x.operation===OLD).length,2);assert.ok(p.typed_scopes.filter(x=>x.operation===OLD).every(x=>x.risk==='high'));const s=p.typed_scopes.filter(x=>x.operation===NEW);assert.equal(s.length,1);assert.deepEqual(s[0],{tool:'control_plane_root_scripts_stage_transport_apply_v1',project:'control_plane',environment:'production',action:'control_plane_root_scripts_stage_transport_v1',risk:'critical',operation:NEW,principals:[{principal_id:'mohammad',roles:['mcp-operator']}]});assert.match(out.mediator,/host_action\.control_plane_installer_refresh_root_stage_v1/);assert.match(out.mediator,/risk:'critical'/);assert.match(out.mediator,/CONFIRM_LEVEL_4_CRITICAL/);assert.doesNotMatch(out.mediator,/risk:'high'|CONFIRM_LEVEL_3_PRODUCTION|level3_confirmation_required/);assert.equal(out.production_mutation,false)});
test('installer is exact-preimage fixed, transactional, rollback-safe and has no caller-controlled path or command',()=>{const m=load(),s=m.buildInstallerSource();assert.equal(m.POLICY_PATH,'/opt/prhm-company-control-plane/config/approval-policy.json');assert.equal(m.MEDIATOR_PATH,'/opt/prhm-company-control-plane/root-scripts-stage-mediator-v1/control-plane-root-scripts-stage-mediator-v1.js');assert.equal(m.POLICY_SHA256,'494e95e3173695407c84b6d082f09e57d971cb193518be813a53131cdb389a70');assert.equal(m.MEDIATOR_SHA256,'e8fc3f5185f01efeca5563490461566f64fc8bda1534bad5a3c39e73a7108abb');assert.equal(m.INSTALLER_SOURCE_SHA256,sha(s));for(const x of [m.POLICY_PATH,m.MEDIATOR_PATH,m.POLICY_SHA256,m.MEDIATOR_SHA256,'host_action.control_plane_installer_refresh_root_stage_v1','prhm-company-approval.service','prhm-root-scripts-stage-mediator-v1.service','/var/backups/prhm-installer-refresh-l4-binding-repair-v1','baseline','rollback','--check','renameSync','is-active'])assert.ok(s.includes(x),x);assert.match(s,/production_mutation:true/);assert.match(s,/database_mutation:false/);assert.doesNotMatch(s,/req\.body|destinationPath|callerContent|process\.argv\[[23]/)});
